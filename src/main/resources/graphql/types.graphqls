# define custom object

enum CustomOrder {
    ASC,
    DESC
}

input CustomSort {
    field: String!
    order: CustomOrder
}

input CustomPaging {
    page: Int!
    size: Int!
    sort: CustomSort
}
